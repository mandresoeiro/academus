# =====================
# 🔐 DJANGO SETTINGS
# =====================

# Chave secreta usada para segurança interna do Django
SECRET_KEY=your-secret-key-here

# Ativa ou desativa o modo debug
DEBUG=True

# Hosts permitidos (em produção, use seu domínio)
ALLOWED_HOSTS=127.0.0.1,localhost

# =====================
# 🗄️ DATABASE (PostgreSQL)
# =====================

POSTGRES_DB=your_db_name
POSTGRES_USER=your_db_user
POSTGRES_PASSWORD=your_db_password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# =====================
# 🌍 CORS (para integração com React/Vue)
# =====================

# Libera requisições de frontend local
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# =====================
# ✉️ EMAIL (usado por allauth/dj-rest-auth)
# =====================

EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=youremail@gmail.com
EMAIL_HOST_PASSWORD=your_app_password_here
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=youremail@gmail.com

# =====================
# 🔐 JWT (caso esteja usando SimpleJWT)
# =====================

JWT_ACCESS_LIFETIME_MINUTES=60
JWT_REFRESH_LIFETIME_DAYS=7

DJANGO_ENV=dev